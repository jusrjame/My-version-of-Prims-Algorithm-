Justin James
README for prims.lp
Contains information necessary to run prims.lp from the command line.

When running clingo from the command line it is possible to change the starting node
by using the following command:

	./clingo prims.lp -c root=<letter of node you wish to start at>